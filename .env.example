# คัดลอกไฟล์นี้เป็น .env แล้วแก้ค่าให้ตรงกับ server
REACT_APP_API_BASE=http://your-server-ip:8000

# Printer (ESC/POS via TCP)
PRINTER_IP=192.168.1.100
PRINTER_PORT=9100

# Shop Info (ใช้ในใบเสร็จ)
SHOP_NAME=ห้างทองจินดา
SHOP_PHONE=0x-xxxx-xxxx
